/* eslint-disable eol-last */
export interface IAtores{
    nome: string;
    cartaz: string;
    filme: string[];
    ocupacao: string;
    pagina?: string; //** a ? indica que o campo nao é obrigatorio*/   
}
  